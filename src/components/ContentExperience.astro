---
interface Education {
    degree: string;
    institution: string;
    duration: string;
    location: string;
    description?: string;
    gpa?: string;
};

interface Experience {
    title: string;
    company: string;
    duration: string;
    location: string;
    description: string;
    technologies?: string[];
};

const education: Education[] = [
  {
    degree: 'Master of Computer Applications (MCA)',
    institution: 'Maulana Azad National Institute of Technology (MANIT), Bhopal',
    duration: '2024 - Present',
    location: 'Bhopal, India',
    description: 'Currently pursuing MCA with a focus on advanced computer science concepts, programming, and software development.'
  },
  {
    degree: 'B.Sc. Physical Science with Computer Science',
    institution: 'University of Delhi',
    duration: '2021 - 2024',
    location: 'Delhi, India',
    description: 'Graduated with a strong foundation in physical sciences and computer science, including programming, algorithms, and mathematics.',
    gpa: '8.99/10'
  }
];

const experience: Experience[] = [];
---

<div class="space-y-8">

    <!-- Education Section -->
    <div>
        <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
            <span class="mr-2">🎓</span>
            Education
        </h2>
        
        <div class="space-y-6">
            {education.map(edu => (
                <div class="border border-gray-700 rounded-lg p-6 bg-black hover:border-gray-600 transition-colors">
                    <div class="flex justify-between items-start mb-2">
                        <h3 class="text-xl font-semibold text-white">
                            {edu.degree}
                        </h3>
                        {edu.gpa && (
                            <span class="text-sm text-gray-400 bg-gray-800 px-2 py-1 rounded">
                                GPA: {edu.gpa}
                            </span>
                        )}
                    </div>
                    
                    <p class="text-primary font-medium mb-1">
                        {edu.institution}
                    </p>
                    
                    <p class="text-gray-400 text-sm mb-2">
                        {edu.duration} • {edu.location}
                    </p>
                    
                    {edu.description && (
                        <p class="text-gray-300 text-sm">
                            {edu.description}
                        </p>
                    )}
                </div>
            ))}
        </div>
    </div>

    <!-- Experience Section -->
    <div>
        <h2 class="text-2xl font-bold text-white mb-6 flex items-center">
            <span class="mr-2">💼</span>
            Experience
        </h2>
        <div class="space-y-6">
            {experience.length === 0 ? (
                <div class="text-gray-400 text-base">No professional experience yet. Currently focused on education and skill development.</div>
            ) : (
                experience.map(exp => (
                    <div class="border border-gray-700 rounded-lg p-6 bg-black hover:border-gray-600 transition-colors">
                        <div class="flex justify-between items-start mb-2">
                            <h3 class="text-xl font-semibold text-white">
                                {exp.title}
                            </h3>
                        </div>
                        <p class="text-primary font-medium mb-1">
                            {exp.company}
                        </p>
                        <p class="text-gray-400 text-sm mb-3">
                            {exp.duration} • {exp.location}
                        </p>
                        <p class="text-gray-300 text-sm mb-3">
                            {exp.description}
                        </p>
                        {exp.technologies && (
                            <div class="flex flex-wrap gap-2">
                                {exp.technologies.map(tech => (
                                    <span class="text-xs bg-gray-800 text-gray-300 px-2 py-1 rounded">
                                        {tech}
                                    </span>
                                ))}
                            </div>
                        )}
                    </div>
                ))
            )}
        </div>
    </div>

</div> 